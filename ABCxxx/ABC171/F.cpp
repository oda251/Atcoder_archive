#include <bits/stdc++.h>

#include <atcoder/all>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < n; ++i)
#define rep2(i, l, r) for (int i = l; i < r; ++i)
auto boostIO = []() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  return 0;
}();
time_t start = clock();
#define cerr_time() \
  cerr << "time: " << (double)(clock() - start) / CLOCKS_PER_SEC << "s" << endl;
#define mint atcoder::modint1000000007
// https://blog.hamayanhamayan.com/entry/2020/06/23/193342
/*####################################################*/
int main() {
  int K;
  string S;
  cin >> K >> S;
  int len = S.size();
  mint ans = 0;
  return 0;
}
