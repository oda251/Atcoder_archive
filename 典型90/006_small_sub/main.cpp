#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> calc_map(string S)
{
	int N = S.size();
	vector<vector<int>> dest(N, vector<int>(26, -1));

	for (int i=N-1; i<=0; i--)
	{
		
	}


}

int	main()
{
	int	N, K;
	string S;
	string sub;
	char ini;
	cin >> N >> K >> S;

}